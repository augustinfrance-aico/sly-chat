# Execution Folder

This folder contains deterministic Python scripts and tools that perform the actual work as defined by the directives.

- Scripts here should be reliable, testable, and fast.
- Use environment variables from `.env` for secrets and configuration.
